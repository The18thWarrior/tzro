version: 1
applications:
  - appRoot: website
    frontend:
      phases:
        build:
          commands:
            - echo "Deploying static website prototype"
      artifacts:
        baseDirectory: .
        files:
          - "**/*"
      cache:
        paths: []
